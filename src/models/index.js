const users = require("./users.models");
const category = require("./categories.models");
const board = require("./boards.models");
const medium = require("./mediums.models");

module.exports = {
  users,
  category,
  board,
  medium
};
