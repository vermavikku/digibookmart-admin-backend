const users = require("./users.schemas");
const category = require("./categories.schemas");
const board = require("./boards.schemas");
const medium = require("./mediums.schemas");
module.exports = {
    users,
    category,
    board,
    medium
}