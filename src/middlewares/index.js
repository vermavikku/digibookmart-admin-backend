const logs_middlewear = require("./log.middlewears");
const authMiddleware = require("./auth.middlewares");
const fileMiddleware = require("./file.middlewares");
module.exports = {
  logs_middlewear,
  authMiddleware,
  fileMiddleware,
};
